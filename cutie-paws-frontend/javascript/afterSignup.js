// This is the file that provides the functionality on the afterCart.html page.
window.onload = redirect;

function redirect() {
   setTimeout(() => {
      window.location.href = "../html/login.html"

   }, 4000);
}